{
  "name": "signalpost-api",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side TikTok OAuth and user-consented Direct Post API for SignalPost",
  "main": "index.js",
  "type": "commonjs",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "lint": "node --check index.js && node --check validation.js"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
