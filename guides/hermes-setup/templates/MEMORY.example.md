# Durable environment facts example — fill locally, do not publish

Only add facts that genuinely apply across sessions.

- Operating environment: YOUR_CONFIRMED_OS_AND_SHELL
- Default workspace: YOUR_LOCAL_WORKSPACE
- Approved external-action policy: YOUR_ACTUAL_POLICY

Reusable procedures belong in skills. Project state belongs in project files. Temporary tasks belong in the task list or session history. Never store credentials here.
